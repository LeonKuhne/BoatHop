#!/bin/sh
title -n BoatHop
debug "minecraft plugin that leaps your boat across hurdles." | indent " | "

help .cd | indent

function build() {
  mvn clean compile assembly:single
  jar=target/boathop-1.0-SNAPSHOT-jar-with-dependencies.jar
  mv $jar BoatHop.jar
}
